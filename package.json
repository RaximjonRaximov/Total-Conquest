{
  "name": "infinite-tower",
  "version": "0.1.0",
  "description": "Infinite Tower — hero-squad tower campaign mobile game (HTML5).",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "license": "ISC",
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  }
}
