# Infinite Tower

Hero-squad tower campaign mobile game (HTML5). Built step by step.

## Stack
- HTML5 + vanilla JS
- [Vite](https://vitejs.dev/) dev server / bundler

## Develop
```bash
npm install
npm run dev      # start dev server (http://localhost:5173)
npm run build    # production build -> dist/
npm run preview  # preview the production build
```

## Status
- **Step 1 (done):** Loading screen — banner + animated 0→100% progress bar, then a start button.

Next steps will add the main menu, hero squad, and the first tower level.
